import { ResultadoPagoTurno } from '@/components/ResultadoPagoTurno';

/** Vuelta del checkout sin escenario declarado (turnos creados antes). */
export default function ReservarPagoPage() {
  return <ResultadoPagoTurno />;
}
